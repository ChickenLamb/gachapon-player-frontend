# Database URL (not currently used, but kept for reference)
DATABASE_URL=

# =============================================================================
# OPTIONAL: Drizzle Kit Remote Operations
# =============================================================================
# Only needed for:
#   - pnpm run db:studio:remote (inspect production database)
#   - pnpm run db:push (push schema directly to remote)
#   - pnpm run db:migrate (apply migrations via Drizzle Kit)
#
# NOT needed for:
#   - pnpm run dev (local development)
#   - pnpm run db:generate (uses drizzle.config.generate.ts - no DB connection)
#   - pnpm run db:studio (uses drizzle.config.local.ts - local SQLite file)
#   - npx wrangler d1 migrations apply (uses wrangler auth)
# =============================================================================

# Get from: npx wrangler whoami
CLOUDFLARE_ACCOUNT_ID=your-account-id-here

# Get from: wrangler.jsonc (database_id field)
# Current value: 6890db87-5cb4-4c96-8cc7-c8fde87ac756
CLOUDFLARE_DATABASE_ID=6890db87-5cb4-4c96-8cc7-c8fde87ac756

# Create at: https://dash.cloudflare.com/profile/api-tokens
# Required permissions: Account > D1 > Edit
CLOUDFLARE_D1_TOKEN=your-api-token-here
